<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>
        <variable
            name="viewModel"
            type="com.example.week5weekendhwmvvm.viewmodel.UserMainActivityViewModel" />
    </data>

    <LinearLayout
        android:padding="12dp"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
        android:background="@color/light_salmon"
    android:layout_width="match_parent"
        android:gravity="center"
    android:layout_height="match_parent">


        <TextView
            android:textColor="@color/white"
            android:layout_margin="4dp"
        android:textSize="24sp"
        android:text="@{viewModel.primLocation}"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>
        <TextView
            android:textColor="@color/white"
            android:textSize="100sp"
            android:text="@{viewModel.primeTemp}"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>
        <TextView
            android:textColor="@color/white"
            android:textSize="34sp"
            android:text="@{viewModel.primeCondition}"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal">
            <TextView
                android:textColor="@color/white"
                android:textSize="20dp"
                android:layout_margin="8dp"
                android:text="@{viewModel.primeHighTemp}"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"/>
            <TextView
                android:textColor="@color/white"
                android:textSize="20dp"
                android:layout_margin="8dp"
                android:text="@{viewModel.primeLowTemp}"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"/>

        </LinearLayout>


</LinearLayout>
</layout>